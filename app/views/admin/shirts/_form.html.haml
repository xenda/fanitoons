- semantic_form_for ["admin",@shirt] do |form|
  - form.inputs do
    = form.input :id
    = form.input :name
    
  - form.buttons do
    = form.commit_button 'Grabar',:button_html => { :class => "submit" }