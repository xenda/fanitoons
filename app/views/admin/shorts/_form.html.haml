- semantic_form_for ["admin",@short] do |form|
  - form.inputs do
    = form.input :id
    = form.input :name
    
  - form.buttons do
    = form.commit_button 'Grabar',:button_html => { :class => "submit" }