- semantic_form_for ["admin",@friend] do |form|
  - form.inputs do
    = form.input :id
    = form.input :name
    = form.input :email
    
  - form.buttons do
    = form.commit_button 'Grabar',:button_html => { :class => "submit" }