- semantic_form_for ["admin",@country] do |form|
  - form.inputs do
    = form.input :name, :label => "Nombre"
    = form.input :iso_name, :label => "Nombre ISO"
  - form.buttons do
    = form.commit_button 'Grabar',:button_html => { :class => "submit" }